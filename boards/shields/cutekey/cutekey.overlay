/*
 * Copyright (c) 2022 The ZMK Contributors
 *
 * SPDX-License-Identifier: MIT
 */

 / {
    chosen {
        zmk,kscan = &default_kscan;
        zmk,battery = &vbatt;
    };

    default_kscan: kscan {
        compatible = "zmk,kscan-gpio-direct";
        label = "default_kscan";
        wakeup-source;                    /* WAKE-ADD */

        input-gpios
            = <&gpio0 29 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>,
              <&gpio0 20 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>,
              <&gpio1 00 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>,
              <&gpio0 22 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>,
              <&gpio1 06 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>, /* CHANGED from P0.02 (ADC collision) to dummy P1.06 */
              <&gpio1 15 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>
            ;
    };

    left_encoder: encoder_left {
        compatible = "alps,ec11";
        label = "encoder";
        a-gpios = <&gpio0 31 (GPIO_ACTIVE_HIGH | GPIO_PULL_UP)>;
        b-gpios = <&gpio0 10 (GPIO_ACTIVE_HIGH | GPIO_PULL_UP)>;
        steps = <80>;
        status = "okay";
    };

    sensors {
        compatible = "zmk,keymap-sensors";
        sensors = <&left_encoder>;
    };

    vbatt: vbatt {
        compatible = "zmk,battery-voltage-divider";
        label = "BATTERY";
        io-channels = <&adc 0>;      /* Switch to P0.02 (AIN0) */
        output-ohms = <100000>;      /* Restore original 100k/200k values */
        full-ohms   = <200000>;
    };

};

/* Enable wake-up on the GPIO controllers that own the keys */
&gpio0 { wakeup-source; };   /* WAKE-ADD */
&gpio1 { wakeup-source; };   /* WAKE-ADD */